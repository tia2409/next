<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-mapper-3.dtd">
<mapper namespace="com.backend.mapper.LoginMapper">

    <select id="loginUser" parameterType="com.backend.login.LoginRequestDTO" resultType="map">
        CALL W_LOGIN(#{userId}, #{password}, #{ipAddr})
    </select>

</mapper>
