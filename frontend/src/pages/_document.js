import { Html, Head, Main, NextScript } from "next/document";

export default function Document() {
  return (
    <Html lang="en" style={{ backgroundColor: "#1e1e1e", color: "#ffffff" }}>
      <Head />
      <body style={{ backgroundColor: "#1e1e1e", color: "#ffffff" }}>
        <Main />
        <NextScript />
      </body>
    </Html>
  );
}
