import { Html, Head, Main, NextScript } from "next/document";

export default function Document() {
  return (
    <Html lang="en" style={{ backgroundColor: "#fff", color: "#000" }}>
      <Head />
      <body style={{ backgroundColor: "#fff", color: "#000" }}>
        <Main />
        <NextScript />
      </body>
    </Html>
  );
}
